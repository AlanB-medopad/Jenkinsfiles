pipeline {
    agent any

    stages {
        stage('Example') {
            steps {               
                
               sh 'pwd'
               sh 'docker run -v ${pwd}/test/:/src ubuntu /src/epigenome-dev.sh'
               sh 'ls'
            }
        }
    }
}